@model IEnumerable<TaskManagementApp.Models.ApplicationUser>
@{
    ViewData["Title"] = "Admin - Users";
}

<div class="container mt-4">
    <h3>Manage Users</h3>
    <table class="table table-hover">
        <thead class="table-dark">
            <tr>
                <th>User</th>
                <th>Email</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.UserName</td>
                    <td>@user.Email</td>
                    <td>
                        @if (user.UserName != User.Identity.Name)
                        {
                            <form asp-action="DeleteUser" asp-route-id="@user.Id" method="post"
                                  onsubmit="return confirm('Are you sure? This action revokes all rights and deletes the user.');">
                                <button type="submit" class="btn btn-danger btn-sm">Delete / Revoke</button>
                            </form>
                        }
                        else
                        {
                            <span class="badge bg-success">You (Admin)</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>